const BlockingObject = function (options) {
  this.pos = options.pos;
  this.dim = options.dim;
  this.color = options.color;
}

BlockingObject.prototype.randomPosition = function () {
  //
}

module.exports = BlockingObject;
